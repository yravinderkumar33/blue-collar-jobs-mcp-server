{
  "name": "mcp-server",
  "version": "1.0.0",
  "description": "MCP Server for Blue Collar Jobs Platform",
  "main": "dist/jobs-mcp.js",
  "scripts": {
    "start": "ts-node src/index.ts",
    "jobs-mcp": "ts-node src/jobs-mcp.ts",
    "build": "tsc --outDir dist",
    "build:jobs-mcp": "tsc src/jobs-mcp.ts --outDir dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["mcp", "jobs", "blue-collar", "employment"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.2",
    "@types/node": "^22.15.29",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "bin": {
    "jobs-mcp": "dist/jobs-mcp.js"
  }
}
